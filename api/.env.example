# Copy to .env for local dev. Real values come from Railway in production.
# Never commit a populated .env (root .gitignore covers *.env).

API_KEY=set-via-openssl-rand-hex-32
DATABASE_URL=postgres://user:pass@host:port/dbname
PORT=3000
